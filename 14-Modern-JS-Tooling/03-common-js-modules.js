// Common JS Modules
// Been used in node.js for all of it's existence
// ES Modules just recently in node
// Almost all modules in npm are common js modules (npm only node initially)

// one file one module

// Export
// Doesn't work in browser, but works in node.js
// export.addToCart = function (product, quantity) {
//     cart.push({ product, quantity });
//     console.log(`${quantity} ${product} added to cart`);
// };

//Import
//const {addToCart} = require('./shoppingCart.js');